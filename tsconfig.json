{
  "compilerOptions": {
    // --- Estrutura de Arquivos e Saída ---
    "rootDir": "src",
    "outDir": "dist",

    // --- Ambiente e Sistema de Módulos (Node.js moderno) ---
    "target": "es2021",
    "lib": ["es2021"],
    "module": "nodenext",
    "moduleResolution": "nodenext",

    // --- Rigor e Boas Práticas ---
    "strict": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "forceConsistentCasingInFileNames": true,

    // --- Interoperabilidade com Módulos JavaScript ---
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "skipLibCheck": true
  },

  // --- Arquivos a Serem Incluídos na Compilação ---
  // Apenas o que está dentro da pasta 'src' será parte do nosso build final.
  // Esta seção fica FORA de "compilerOptions".
  "include": [
    "src/**/*"
  ],

  // --- Arquivos a Serem Excluídos (boa prática) ---
  "exclude": [
    "node_modules"
  ]
}